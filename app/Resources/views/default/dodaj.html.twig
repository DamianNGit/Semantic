{% extends 'base.html.twig' %}

{% block body %}

    <h2 class="top">Dodaj pracownika</h2>

    <div class="row top">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">

            {{ form_start(form) }}
                {{ form_widget(form) }}
                    <button class="btn btn-success">Dodaj</button>
            {{ form_end(form) }}

        </div>
        <div class="col-sm-2"></div>
    </div>

{%  endblock %}