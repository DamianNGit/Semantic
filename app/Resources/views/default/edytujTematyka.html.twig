{% extends 'base.html.twig' %}

{% block body %}

    <h2 class="top">Edytuj tematykę badawczą</h2>

    <div class="row">
        <div class="col-sm-2"></div>
        <div class="col-sm-8">

            <h3 class="top">{{ dydaktyka.tytul }}</h3>

            {{ form_start(dydaktykaForm) }}
            {{ form_widget(dydaktykaForm) }}
            <button class="btn btn-success" type="submit">Zapisz</button>
            {{ form_end(dydaktykaForm) }}

        </div>
        <div class="col-sm-2"></div>
    </div>

{% endblock %}