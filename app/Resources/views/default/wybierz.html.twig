{% extends 'base.html.twig' %}


{% block body %}

    <h2 class="top">Wybierz pracownika do edycji</h2>

    <div class="row top">
        <div class="col-sm-4"></div>
        <div class="col-sm-4">
            {% for rekord in stronaW %}

                <div>
                    <h2><a class="rekordPracownik" href="{{ path('edit_pracownik', {'id':rekord.id}) }}">{{ rekord.tytul }}</a></h2>
                    <hr>
                </div>

            {% endfor %}

            <div class="navigation bottN">
                {{ knp_pagination_render(stronaW) }}
            </div>
        </div>
        <div class="col-sm-4"></div>
    </div>

{% endblock %}